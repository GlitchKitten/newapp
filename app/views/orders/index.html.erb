<h2> Order Index Page</h2>
<table>
  <thead>
    <tr>
      <th>User ID</th>
      <th>Product ID</th>
      <th>Total</th>
      <th colspan="2">Actions</th>
    </tr> 
   <thead> 
  
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.user_id %></td>
        <td><%= order.product_id %></td>
        <td><%= order.total %></td>
        <td><%= link_to 'Show', order %></td>
        <td><%= link_to 'New', new_order_path(order) %></td>
      </tr>
    <% end %>
  </tbody>
  
</table>